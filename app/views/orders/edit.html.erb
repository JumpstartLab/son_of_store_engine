<%= form_for([@order.store, @order]) do |o| %>
<p>
  Order Status: <%= select('order', 'status', 
                    %w(pending cancelled shipped paid returned)) %>
</p>

<p>
  <%= o.submit %> 
</p>

<% end %>