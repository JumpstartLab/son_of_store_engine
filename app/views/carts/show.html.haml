%h1 Your Cart
%br
  = render :partial => "cart"
  - if current_user.present?
    = link_to "Checkout", new_order_path,
        :class => "a btn btn-primary right big btn-large"
  - else
    = link_to "Checkout", new_session_path,
      :class => "a btn btn-primary right big btn-large"