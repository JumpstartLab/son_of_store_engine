table.table.table-bordered.table-striped
  theader
    tr
      th Order Number
      th Purchase Date
      th Status
      th Order Total
  - @orders.each do |order|
    tr
      td= link_to "Order #{order.id}", order_path(store_slug, order.id)
      td= order.created_at.to_s(:month_day_and_year)
      td= order.status
      td= order.order_total
