<h1>PRODUCTS INDEX</h1>

<% @products.each do |product| %>
  <%= link_to product.title, product_path(product), :method => "delete" %> ||
  <%= link_to "Delete", product_path(product), :method => "delete" %><br>
<% end %> 

<%= link_to "Create New Product", new_product_path %>
