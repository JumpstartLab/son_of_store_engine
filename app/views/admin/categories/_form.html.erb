<%= form_for([:admin, @category]) do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.hidden_field :store_id, :value => store.id %>
  </div>

  <div class="actions"><br />
    <%= f.submit %>
  </div>

<% end %>
