<tr>
  <td><%= "Order ##{admin_order.id}" %></td>
  <td><%= link_to "#{admin_order.user.name}", admin_order_path(store_slug: store_slug, id: admin_order.id) %></td>
  <td><%= admin_order.created_at %></td>
  <td><%= admin_order.status %></td>
  <td><%= order_actions_by_status(admin_order) %></td>
</tr>